## Introducción

Ahora vamos a tener un tiempo regular en el que vamos a actualizar el estado del sistema. El mismo consiste de $N$ partículas que interactúan mediante fuerzas que en general dependen de la distancia entre ellas. Se contempla una integración numérica de las ecuaciones de movimiento. El tiempo avanza en cantidades discretas $dt$ (el paso temporal). Las interacciones pueden ser de largo o corto alcance. En cualquier caso los "choques" no son instantáneos sino que tienen una duración de varios pasos temporales.

Este esquema es útil en el caso de partículas en contacto la mayor parte del tiempo:
- O bien interacciones largo alcance.
- O bien de corto alcance pero con alta densidad.

También en los casos donde tiempo de vuelo (entre choques) $<<$ duración del choque.
![[Pasted image 20260724230649.png|center|450]]

---
## Desarrollo de Taylor

El espíritu de este desarrollo es tomar una función y aproximarla en un entorno mediante un polinomio de un grado arbitrario.
$$\begin{gathered}f(x) \cong f(a) + \frac{f'(a)}{1!}(x-a) + \frac{f''(a)}{2!}(x-a)^2 + \frac{f'''(a)}{3!}(x-a)^3 + \dots \\[2ex]
\boxed{f(x) = \sum_{n=0}^{\infty} \frac{f^{(n)}(a)}{n!}(x-a)^n} \end{gathered}$$

Gráficamente se ve de la siguiente manera:
![[Pasted image 20260725192753.png|center|547]]

Vemos que a mayor orden, cada vez se acorta más la “distancia” a la función.

---
## Algoritmos de Euler

Para resolver las ecuaciones diferenciales de la evolución del sistema, vamos a tomar una serie de algoritmos. En primer lugar, podemos contemplar la fuerza y realizar lo siguiente:
$$
\begin{gathered}
\mathbf{r}_i(t + \Delta t) = \mathbf{r}_i(t) + \Delta t \, \mathbf{v}_i(t) + \frac{\Delta t^2}{2m_i} \mathbf{f}_i(t) + \mathcal{O}(\Delta t^3) \\[2ex]
\mathbf{v}_i(t + \Delta t) = \mathbf{v}_i(t) + \frac{\Delta t}{m_i} \mathbf{f}_i(t) + \mathcal{O}(\Delta t^2)
\end{gathered}
$$

Pero esto es poco preciso. En este caso se realiza un desarrollo de Taylor a partir del $t$ conocido en un $dt$. Vemos por ejemplo que en el segundo término se tiene la velocidad, ya que es la derivada de la posición en función del tiempo.

Una modificación consiste en usar la velocidad ya actualizada en lugar de la del paso anterior.
$$
\begin{gathered}
\mathbf{v}_i(t + \Delta t) = \mathbf{v}_i(t) + \frac{\Delta t}{m_i} \mathbf{f}_i(t) \\[2ex]
\mathbf{r}_i(t + \Delta t) = \mathbf{r}_i(t) + \Delta t \, \mathbf{v}_i(t + \Delta t) + \Delta t^2 \mathbf{f}_i(t) / 2m_i
\end{gathered}
$$

En este caso, se hace la misma cuenta (en cuanto a costo computacional) y se tiene una mejoría. El orden distinto en el cálculo nos brinda un poco más de precisión.

---
## Algoritmos de Verlet

Se hace una idea similar, utilizando un desarrollo de Taylor pero se contempla hasta delta t al cubo. Se puede hacer el siguiente “truco”:
$$
\begin{gathered}
\mathbf{r}_i(t + \Delta t) = \mathbf{r}_i(t) + \Delta t \, \mathbf{v}_i(t) + \frac{\Delta t^2}{2m_i} \mathbf{f}_i(t) + \frac{\Delta t^3}{3!} \mathbf{\ddot{r}}_i(t) + \mathcal{O}(\Delta t^4) \\[2ex]
\mathbf{r}_i(t - \Delta t) = \mathbf{r}_i(t) - \Delta t \, \mathbf{v}_i(t) + \frac{\Delta t^2}{2m_i} \mathbf{f}_i(t) - \frac{\Delta t^3}{3!} \mathbf{\ddot{r}}_i(t) + \mathcal{O}(\Delta t^4) \\[2ex]
\Longrightarrow \quad \mathbf{r}_i(t + \Delta t) = 2\mathbf{r}_i(t) - \mathbf{r}_i(t - \Delta t) + \frac{\Delta t^2}{m_i} \mathbf{f}_i(t) + \mathcal{O}(\Delta t^4)
\end{gathered}
$$

La idea central es escribir la posición de la partícula $r_i$ en los instantes $t+\Delta t$ y $t-\Delta t$. Cada una de estas expresiones se expande alrededor de $t$, incluyendo términos de velocidad, fuerza (aceleración) y derivadas de orden superior. Luego, al sumar ambas expresiones, los términos lineales en la velocidad ($\pm \Delta t v_i(t)$) se cancelan, al igual que los términos de tercer orden, quedando únicamente los términos de posición en $t$, las aceleraciones multiplicadas por $\Delta t^2$ y un error de orden $\mathcal{O}(\Delta t^4)$.

Este procedimiento es ingenioso porque elimina la dependencia directa de la velocidad y permite avanzar la posición usando solo las posiciones previas y las fuerzas actuales, garantizando una integración más estable y con error global del orden $\Delta t^2$. Por eso el método de Verlet es muy popular en simulaciones de dinámica molecular: es sencillo, conservativo y no requiere almacenar ni calcular explícitamente las velocidades en cada paso.

En la variante **Leap-Frog** (viene del juego que una persona se pone en cuclillas y la otra la salta por arriba) se calcula las velocidades y posiciones en tiempos desfasados.
$$
\begin{gathered}
\mathbf{v}_i\left(t + \frac{\Delta t}{2}\right) = \mathbf{v}_i\left(t - \frac{\Delta t}{2}\right) + \frac{\Delta t}{m_i} \mathbf{f}_i(t), \\[2ex]
\mathbf{r}_i(t + \Delta t) = \mathbf{r}_i(t) + \Delta t \, \mathbf{v}_i\left(t + \frac{\Delta t}{2}\right).
\end{gathered}
$$

Donde la velocidad puede calcularse como:
$$\mathbf{v}_i(t) = \frac{\mathbf{v}_i\left(t - \frac{\Delta t}{2}\right) + \mathbf{v}_i\left(t + \frac{\Delta t}{2}\right)}{2}.$$

Esto lo hacemos porque queremos “engañar” al sistema. En lugar de tomar un paso $dt$, estamos tomando un paso $\frac{dt}{2}$. Achicar el paso mejora la aproximación, como vimos en el desarrollo de Taylor. No lo achicamos numéricamente porque si tenemos $dt$ muy chicos, vamos a tener que hacer muchísimos más pasos.

Las desventajas es que se tienen estas dos variables en tiempos distintos, por lo que se debe hacer una transformación en la velocidad con los promedios para ajustarla.

Otra es **Velocity-Verlet**, donde también se hacen pasos intermedios con $dt$s más pequeños.
$$
\begin{gathered}
\mathbf{r}_i(t + \Delta t) = \mathbf{r}_i(t) + \Delta t \, \mathbf{v}_i(t) + \frac{\Delta t^2}{m_i} \mathbf{f}_i(t) + \mathcal{O}(\Delta t^3), \\[2ex]
\mathbf{v}_i(t + \Delta t) = \mathbf{v}(t) + \frac{\Delta t}{2m_i} (\mathbf{f}_i(t) + \mathbf{f}_i(t + \Delta t)) + \mathcal{O}(\Delta t^2).
\end{gathered}
$$

La idea es avanzar primero la posición usando la velocidad actual y la aceleración conocida, luego calcular la nueva aceleración a partir de la fuerza en el tiempo actualizado, y finalmente corregir la velocidad combinando la aceleración inicial y la final. Esto se interpreta como un esquema en dos pasos: primero se calcula una velocidad intermedia que sirve para mover la partícula, y después, con la fuerza en el nuevo tiempo, se termina de ajustar la velocidad. De esta forma se logra un algoritmo estable, que conserva bien la energía en el tiempo y que además entrega velocidades confiables, algo que el Verlet original no hacía directamente.

Podemos ver el siguiente esquema para entender bien los integradores estudiados:
![[Pasted image 20260725193313.png|center|557]]

Otro algoritmo es el de **Beeman**, donde se evalúa la función aceleración en distintos pasos.
$$
\begin{gathered}
\mathbf{r}(t + \Delta t) = \mathbf{r}(t) + \mathbf{v}(t)(\Delta t) + \frac{2}{3}\mathbf{a}(t)\Delta t^2 - \frac{1}{6}\mathbf{a}(t - \Delta t)\Delta t^2 \\[2ex]
\mathbf{v}(t + \Delta t) = \mathbf{v}(t) + \frac{1}{3}\mathbf{a}(t + \Delta t)\Delta t + \frac{5}{6}\mathbf{a}(t)\Delta t - \frac{1}{6}\mathbf{a}(t - \Delta t)\Delta t
\end{gathered}
$$

La idea principal es que no solo se usan la posición, velocidad y aceleración en el tiempo actual, sino también la aceleración del paso anterior. De este modo se incorpora más información histórica, lo que reduce el error en el desarrollo de Taylor.

En la práctica, Beeman utiliza una combinación lineal de las aceleraciones presente y pasada para predecir la nueva posición. Una vez calculada la posición en el nuevo tiempo, se obtiene la aceleración correspondiente y se corrige la velocidad con un promedio ponderado de las aceleraciones pasada, presente y futura.

La ventaja es que el error en las posiciones se mantiene del orden de $\Delta t^4$, mientras que el de las velocidades mejora respecto a Verlet clásico y Velocity Verlet, quedando en el orden de $\Delta t^3$. Esto hace que el método conserve mejor las cantidades físicas a lo largo del tiempo, como la energía total en simulaciones de dinámica molecular. Otra ventaja es que podríamos contemplar aceleraciones que dependan de la velocidad, cosa que antes no hacíamos, era en base a las posiciones.

![[Pasted image 20260725193442.png|center]]

---
## Algoritmos Predictor-Corrector

La idea es predecir las posiciones y velocidades en un tiempo futuro para conocer las fuerzas futuras y así hacer las correcciones a nuestras predicciones. Tomemos el primer caso, el **Algoritmo Euler Predictor-Corrector**:
1. Predecir :
$$
\begin{gathered}
\mathbf{v}_i^p(t+\Delta t) = \mathbf{v}_i(t) + \mathbf{a}_i(t)\Delta t \\[2ex]
\mathbf{r}_i^p(t+\Delta t) = \mathbf{r}_i(t) + \mathbf{v}_i(t)\Delta t
\end{gathered}
$$
2. Evaluar :
$$\mathbf{f}_i(\mathbf{r}_i^p, \mathbf{v}_i^p) \Rightarrow \mathbf{a}_i(t+\Delta t)$$
3. Corregir :
$$
\begin{gathered}
\mathbf{v}_i(t+\Delta t) = \mathbf{v}_i(t) + \mathbf{a}_i(t+\Delta t)\Delta t \\[2ex]
\mathbf{r}_i(t+\Delta t) = \mathbf{r}_i(t) + \mathbf{v}_i(t+\Delta t)\Delta t
\end{gathered}
$$

El **Algoritmo Gear Predictor-Corrector** es el siguiente, donde se realiza un desarrollo de Taylor de un orden arbitrario de derivadas. Tomamos como notación $\mathbf{r}_q = \frac{d^q\mathbf{r}}{dt^q}$.
1. Predecir: La $p$ superíndice significa que es predicha, y el subíndice $q$ es el orden de derivada.
$$
\begin{gathered}
\mathbf{r}^p(t + \Delta t) = \mathbf{r}(t) + \mathbf{r}_1(t)(\Delta t) + \mathbf{r}_2(t)\frac{(\Delta t)^2}{2!} + \mathbf{r}_3(t)\frac{(\Delta t)^3}{3!} + \mathbf{r}_4(t)\frac{(\Delta t)^4}{4!} + \mathbf{r}_5(t)\frac{(\Delta t)^5}{5!} \\[2ex]
\mathbf{r}_1^p(t + \Delta t) = \mathbf{r}_1(t) + \mathbf{r}_2(t)(\Delta t) + \mathbf{r}_3(t)\frac{(\Delta t)^2}{2!} + \mathbf{r}_4(t)\frac{(\Delta t)^3}{3!} + \mathbf{r}_5(t)\frac{(\Delta t)^4}{4!} \\[2ex]
\mathbf{r}_2^p(t + \Delta t) = \mathbf{r}_2(t) + \mathbf{r}_3(t)(\Delta t) + \mathbf{r}_4(t)\frac{(\Delta t)^2}{2!} + \mathbf{r}_5(t)\frac{(\Delta t)^3}{3!} \\[2ex]
\mathbf{r}_3^p(t + \Delta t) = \mathbf{r}_3(t) + \mathbf{r}_4(t)(\Delta t) + \mathbf{r}_5(t)\frac{(\Delta t)^2}{2!} \\[2ex]
\mathbf{r}_4^p(t + \Delta t) = \mathbf{r}_4(t) + \mathbf{r}_5(t)(\Delta t) \\[2ex]
\mathbf{r}_5^p(t + \Delta t) = \mathbf{r}_5(t)
\end{gathered}
$$
2. Evaluar: Evalúo la Fuerza($t+\Delta t$) con las variables predichas y obtengo la aceleración $\mathbf{a}(t+\Delta t)$, luego definimos la siguiente fórmula, donde el $\Delta R$ a a ser nuestro factor de corrección, como vemos a continuación.
$$
\begin{gathered}
\Delta\mathbf{a} = \Delta\mathbf{r}_2 = \mathbf{a}(t+\Delta t) - \mathbf{a}^p(t+\Delta t) = \mathbf{r}_2(t+\Delta t) - \mathbf{r}_2^p(t+\Delta t) \\[2ex]
\mathbf{\Delta R2} = \frac{\Delta\mathbf{a}(\Delta t)^2}{2!}
\end{gathered}
$$
3. Obtengo las variables corregidas: $\mathbf{r}_q^c$
$$
\begin{gathered}
\mathbf{r}^c = \mathbf{r}^p + \alpha_0 \, \mathbf{\Delta R2} \\[2ex]
\mathbf{r}_1^c = \mathbf{r}_1^p + \alpha_1 \, \mathbf{\Delta R2} / (\Delta t) \\[2ex]
\dots \\[2ex]
\mathbf{r}_q^c \frac{(\Delta t)^q}{q!} = \mathbf{r}_q^p \frac{(\Delta t)^q}{q!} + \alpha_q \mathbf{\Delta R2} \\[2ex]
\text{O despejando:} \\[2ex]
\mathbf{r}_q^c = \mathbf{r}_q^p + \alpha_q \mathbf{\Delta R2} \frac{q!}{(\Delta t)^q}
\end{gathered}
$$

El $\alpha$ es un número llamado **Coeficiente del Predictor Gear**, se encuentra en una tabla y depende del integrador. En base al orden tomado, se debe ir a ella y tomar el valor correspondiente. Para el caso $r_2 = f(r)$, las fuerzas solo dependen de las posiciones:
![[Pasted image 20260725194111.png|center|422]]

Para el caso $r_2 = f(r, r_1)$, las fuerzas dependen de las posiciones y las velocidades.
![[Pasted image 20260725194147.png|center|425]]

Veamos ahora el primer paso de integración. Las derivadas de la posición de orden mayores a 2 se obtienen derivando la expresión de la Fuerza. Por ejemplo en el caso de 1 partícula con fuerza elástica $\mathbf{F} = m\,\mathbf{r}_2 = -k\,(\mathbf{r}-\mathbf{r}^0)$ serán (S.E.U.O.):
$$
\begin{gathered}
\mathbf{r}_2 = -k/m \, (\mathbf{r}-\mathbf{r}^0) \\[2ex]
\mathbf{r}_3 = -k/m \, \mathbf{r}_1 \; ; \\[2ex]
\mathbf{r}_4 = -k/m \, \mathbf{r}_2 = (k/m)^2 \, (\mathbf{r}-\mathbf{r}^0); \\[2ex]
\mathbf{r}_5 = -k/m \, \mathbf{r}_3 = (k/m)^2 \, \mathbf{r}_1
\end{gathered}
$$

En los casos de fuerzas más complejas, se puede inicializar las derivadas superiores en cero.

---
## Elección del Paso Temporal

Es importante diferenciar el $dt$ del sistema y el que se usa para realizar los guardados de datos de la simulación, es decir, el estado del sistema.

Si es muy corto, dijimos que la simulación va a requerir demasiadas iteraciones. Si es muy largo, se iban a tener pérdidas o incluso inconsistencias en las posiciones de las partículas. Entonces, vamos a intentar cuantificar el error que estamos cometiendo en la resolución del sistema.

Un caso simple es comparar con la solución analítica. Un caso realista es, si el sistema es conservativo, definir una métrica como por ejemplo:
$$\text{E}_{\text{cinética}} + \text{E}_{\text{potencial}} = \text{Cte.}$$

Para los sistemas no conservativos podemos repetir simulaciones con $dt$s cada vez menores hasta que los resultados cambien menos que un dado error.

---
## Casos de Estudio

En primer lugar, podemos tomar un **oscilador amortiguado** con sólo una partícula. Comparamos con la solución analítica. $$f = ma = mr_2 = -kr - \gamma r_1$$Con $m = 70\text{ kg}$; $k = 10^4\text{ N/m}$; $\gamma = 100\text{ kg/s}$; y condiciones iniciales: $t_f = 5\text{ s}$; $r(t=0) = 1\text{ m}$; $v(t=0) = - A\,\gamma / (2m)\text{ m/s}$. La solución es: $$r = A \, \exp\left(-\left(\frac{\gamma}{2m}\right)t\right) \, \cos\left(\left(\frac{k}{m} - \frac{\gamma^2}{4m^2}\right)^{0.5} t\right)$$![[Pasted image 20260725194509.png|center|573]]

Otro caso es el **gas de Lennard-Jones**, donde el potencial LJ 12-6 sirve para aproximar cómo interactúan dos átomos o moléculas neutros entre sí cuando se van acercando.
$$V_{LJ} = 4\epsilon \left[ \left(\frac{\sigma}{r}\right)^{12} - \left(\frac{\sigma}{r}\right)^6 \right] = \epsilon \left[ \left(\frac{r_m}{r}\right)^{12} - 2\left(\frac{r_m}{r}\right)^6 \right]$$
Donde:
* $\epsilon$ es la profundidad del pozo.
* $r$ es la distancia entre partículas.
* $\sigma$ es la distancia a la cual el potencial se hace cero.
* $r_m$ es la distancia a la cual se encuentra el mínimo ($r_m = 2^{1/6}\sigma$).

También se tiene el **sistema gravitatorio**:

| Fuerza de Gravedad | Energía Potencial |
| :---: | :---: |
| $\mathbf{F}_{ij} = G \, \frac{m_i \, m_j}{r_{ij}^2} \, \mathbf{e}_{ij}$ | $E_{ij}^{pot} = -G \, \frac{m_i \, m_j}{r_{ij}}$ |

Donde:
* $m$ es la masa de cada partícula.
* $r_{ij}$ es la distancia entre las partículas $i$ y la $j$.
* $G$ es la constante universal de gravitación: $G = 6,693 \cdot 10^{-11} \frac{\text{m}^3}{\text{kg} \cdot \text{s}^2}$

Si tuviéramos partículas puntuales con masa, al tener distancias muy chicas nos podría romper todo el sistema y dar una fuerza muy grande, por lo que hay que prestar atención a la aclaración del TP.

Recordemos que las fuerzas se suman:
![[Pasted image 20260724230649.png|center|497]]

Antes de sumar fuerzas, para cada partícula, conviene proyectar la normal y la tangencial (generadas por cada una de las demás partículas en contacto) en las componentes cartesianas $(x,y)$.
$$
\begin{gathered}
F_x = F_N \; e_x^n \\[2ex]
F_y = F_N \; e_y^n
\end{gathered}
$$
Donde:
$$
\begin{gathered}
e_x^n = (x_j - x_i) / |\mathbf{r}_j - \mathbf{r}_i| \\[2ex]
e_y^n = (y_j - y_i) / |\mathbf{r}_j - \mathbf{r}_i|
\end{gathered}
$$

Además, las componentes tangenciales:
$$
\begin{gathered}
e_x^t = - e_y^n \\[2ex]
e_y^t = e_x^n
\end{gathered}
$$

Vamos a tener que resolver 3n sistemas. Finalmente la fuerza sobre cada partícula i debido la interacción con las demás partículas (j) resulta:
$$
\mathbf{F}_i^{Tot} = \sum_{j} \mathbf{F}_{Nij} \quad \Longrightarrow \quad
\begin{gathered}
{F_i^T}_x^{ot} = \sum_{j} F_{Nij} \, {e_x^n}_{ij} \\[2ex]
{F_i^T}_y^{ot} = \sum_{j} F_{Nij} \, {e_y^n}_{ij}
\end{gathered}
$$
