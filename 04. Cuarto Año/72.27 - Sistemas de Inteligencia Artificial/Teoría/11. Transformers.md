## Introducción

Los **transformers** fueron propuestos en el famoso trabajo *Attention Is All You Need*. Su propuesta era la conclusión en ese momento de muchos años de trabajo e intentos para implementar un mecanismo adecuado para el procesamiento del lenguaje natural, NLP (Natural Language Processing), el procesamiento de tokens de texto, en una secuencia, y solucionando los problemas del vanishing/exploding gradient, VGP, de las redes recurrentes y los problemas de las redes LSTM. 

---
## Arquitectura de Transformers

Los **transformers** son entonces redes neuronales utilizadas para transformar una secuencia en otra basada en un mecanismo de atención. La arquitectura general es:
![[Pasted image 20260713135832.png|center|541]]

Los transformers fueron diseñados para interpretar secuencias de tokens en texto escrito. Tienen una arquitectura general determinada por un _encoder_ y un _decoder_, nomenclatura que quizás fue influenciada por la arquitectura de los autoencoders, aunque sin ningún tipo de relación directa. El objetivo del _encoder_ es procesar las entradas, mientras que el _decoder_ recibe información tanto de la entrada como de la salida generada hasta ese momento.

Al pensar al Transformer como una arquitectura para resolver la traducción de un texto de un lenguaje a otro, aparece la justificación de los diferentes componentes.
- **Encoder**: El encoder recibe la información de la cadena de entrada. Tiene dos componentes, el primero corresponde a una capa que implementa el **Mecanismo de Atención (Self-Attention)**, que relacionará los lexemas de la entrada entre sí. La segunda capa es un MLP. La salida pasa al encoder subsiguiente del stack.
- **Decoder**: La salida final del último encoder es la entrada a cada uno de los decoders, y reciben una entrada adicional que viene de la salida hasta el momento para el primer decoder, y de la salida del decoder anterior para todos los otros. El decoder tiene una capa de Self-Attention, luego una capa de encoder-decoder-attention (que toma la salida de la capa anterior versus las entradas recibidas del encoder) y finalmente un MLP. La salida final del último decoder genera la salida actual, en este caso el token actual de traducción.

---
## Embedding

Los **embeddings** son los diferentes esquemas de codificación de los lexemas. El primer proceso consiste en dividir la entrada en componentes. En lenguajes como inglés o español la división es más directa ya que por ejemplo se podría dividir la entrada en palabras. Este es un proceso denominado tokenización. 

Las **palabras (o tokens)** se transforman en números en base a un diccionario global de codificación. Luego, cada token es representado en un vector numérico de dimensión fija, capturando relaciones semánticas y sintácticas en función de su contexto en el lenguaje. Para ello se aplica un algoritmo de embedding. Existen varias alternativas como WordToVec, CBOW, Bag of Words o incluso SVD.3.
![[Pasted image 20260713134210.png|center|380]]

Este esquema tiene el problema de que el orden es invariante a las permutaciones, lo cuál sabemos que no es algo característico del lenguaje. Por lo tanto también se requiere que la codificación tenga en cuenta la ubicación del token en la secuencia.

---
## Attention Is All You Need

El **Mecanismo de Atención** es central en los transformers hasta el punto que es casi un sinónimo que representa principalmente el detalle más fundamental de la arquitectura. Es una capa interna que poseen todos los encoders y decoders, cuya función es tener en cuenta otras palabras de la secuencia mientras se está analizando una en particular. 

En el caso de los encoders, esta capa le permite poner el foco en cualquier otra palabra de la entrada, en tanto que los decoder únicamente pueden analizar palabras previas en la secuencia de salida. La capa de atención permite relacionar los lexemas del lenguaje entre sí. Por ejemplo, de la secuencia "Yo soy un estudiante", la palabra "estudiante" está referenciando el pronombre "Yo". Por eso los pesos, los parámetros libres en el mecanismo de atención, deberían ser más fuertes entre estas dos palabras. 

El cálculo de self-attention es el siguiente:
1. Por cada embedding $x_i$, se calculan 3 vectores llamados query, key y value multiplicando los embeddings por 3 matrices de pesos (Attention Weights) $WQ$, $WK$ y $WV$.
![[Pasted image 20260713133624.png|center|279]]
2. Se calcula un score por cada embedding que determina cuánta atención se le pone a ese embedding mientras se está procesando otro. Se hace el producto escalar entre $q$ y el vector $k$ de cada embedding.
![[Pasted image 20260713133740.png|center|283]]
3. Se dividen los scores por la raíz de la dimensión de vector $k(dy)$
4. Usando Softmax para los scores se obtienen probabilidades $(p.)$.
5. Se multiplica cada vector $v$ por lo obtenido en la función de Softmax.
6. Se suman los vectores ponderados $v'$ obteniendo el output de la capa de Self-Attention para cada token (en este ejemplo, para el primero).
![[Pasted image 20260713133858.png|center|353]]

Para implementarlo se calcula de forma matricial para un procesamiento más rápido:
$$
\text{Attention}(K, Q, V) = \text{SoftMax}\left( \frac{QK'}{\sqrt{d_k}} \right) * V = Z
$$
- $Q$ (Queries/Consultas): Representa la palabra o token que se está analizando actualmente y que busca información en el resto de la secuencia.
- $K$ (Keys/Claves): Representa las etiquetas o perfiles de todos los tokens de la secuencia con los cuales se va a comparar la consulta ($Q$).
- $V$ (Values/Valores): Contiene la información real de los tokens, la cual se pondera y extrae una vez que se determina la relevancia de cada uno.
- **$d_k$:** Es la dimensión de los vectores de las claves ($K$), utilizada en el denominador como un factor de escala ($\sqrt{d_k}$) para evitar que el producto punto crezca demasiado y sature la función SoftMax.

Con la definición de la función de activación SoftMax como:
$$
\sigma(\vec{z})_i = \frac{e^{z_i}}{\sum_{j=1}^{K} e^{z_j}}
$$
- **$\sigma(\vec{z})_i$:** Es el peso de atención normalizado para el $i$-ésimo token, expresado como un valor entre 0 y 1.
- **$z_i$:** Representa la puntuación de afinidad o compatibilidad (el resultado de $\frac{Q K^T}{\sqrt{d_k}}$) obtenida para el token $i$.
- **$\sum_{j=1}^{K} e^{z_j}$:** Es la suma de las exponenciales de todas las puntuaciones del conjunto, la cual actúa como término de normalización para asegurar que la suma de todos los pesos de atención resultantes sea exactamente igual a 1 (formando una distribución de probabilidad).

El mecanismo de **Multi-Headed Attention** surge como una arquitectura diseñada específicamente para mejorar la performance de la capa de self-attention. La ventaja fundamental de este enfoque radica en que permite al encoder enfocarse en distintas posiciones de la secuencia de manera simultánea, logrando así capturar múltiples tipos de relaciones semánticas y sintácticas al mismo tiempo. Para alcanzar esta capacidad, el algoritmo no se limita a una única representación de atención, sino que opera con múltiples conjuntos paralelos de proyecciones de Claves ($K)$, Consultas ($Q$) y Valores ($V$). Cada uno de estos subconjuntos independientes constituye un head (o cabezal, numerados desde Head $0$ hasta Head $n$), los cuales procesan la información en paralelo para enriquecer profundamente la codificación final del lenguaje.
![[Pasted image 20260713134617.png|center|522]]

Notemos ahora que el mecanismo de atención procesa todos los tokens en paralelo, lo que significa que la red no tiene noción del orden ni de la posición de las palabras en la secuencia. Una posible solución es agregar información a cada palabra sobre su posición en la oración. A esta información se la llama **Positional Encoding**, lo que es un conjunto de $p_i$ valores que se le suma a cada embedding. Si bien hay diversas formas de abordar este inconveniente, una de ellas es utilizar las siguientes funciones sinusoidales para obtener el vector de posición del token $i$:
$$ \begin{gathered}
PE_{(pos, 2i)} = \sin\left(\frac{pos}{10000^{\frac{2i}{d}}}\right) \\[2ex]
PE_{(pos, 2i + 1)} = \cos\left(\frac{pos}{10000^{\frac{2i}{d}}}\right)
\end{gathered}$$
donde
* $pos$ es la posición del token en la secuencia.
* $i$ es el índice del vector de posición.
* $d$ es la dimensión de un embedding, la cual será la misma que un vector de posición $p_i$.

![[Pasted image 20260713134745.png|center|542]]

Podemos verlo gráficamente:
![[Pasted image 20260713135152.png|center|340]]

Luego de pasar por los encoders y decoders, se deben traducir los números generados a palabras, por lo que se agregan dos nuevas capas, linear y softmax. La **capa linear** es una red neuronal fully-connected que proyecta el output del último decoder en un vector más grande llamado logits, que otorga un puntaje para cada token para luego, mediante **softmax**, indicar cual es el más probable que sea el siguiente. 
![[Pasted image 20260713135303.png|center|436]]

---
## Entrenamiento

Para entender el proceso de **entrenamiento** de un transformer, veamos un caso de uso. Se quiere traducir "gracias" a "thanks". Se tiene un vocabulario de salida"
$$\text{v} = (\text{a}, \text{am}, \text{i}, \text{thanks}, \text{student}, \text{<eof>})$$
Entonces, lo que vamos a querer hacer es maximizar la probabilidad de que sea efectivamente la palabra "thanks" y minimizar la de que sea cualquier otra palabra. Los pesos se inicializan de forma aleatoria, entonces el modelo sin entrenar devuelve una distribución de probabilidades con valores arbitrarios para cada token. Se pueden comparar las probabilidades y por backpropagation nos vamos acercando al output deseado.
![[Pasted image 20260713135540.png|center|470]]

Entrenar un transformer requiere de gran costo computacional. Por ejemplo, en el siguiente caso donde se entrenó un transformer para traducir del inglés al alemán, se requirieron 3.5 días de procesamiento en 8 GPUs. 
![[Pasted image 20260713135611.png|center|516]]

